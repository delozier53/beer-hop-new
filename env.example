# ---- Required (server) ----
# Postgres connection string (Supabase works). Must include sslmode=require for Supabase.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Supabase project URL and keys
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# Client-safe key. Used ONLY in client/.env with VITE_ prefix; do not use here.
SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
# Server-only key. NEVER expose to client.
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Supabase Storage
SUPABASE_STORAGE_BUCKET=beerhop-assets

# ---- Optional (server) ----
# If you send email (passwordless links, etc.)
SENDGRID_API_KEY=your-sendgrid-api-key

# Port the Node server binds to (Render provides PORT automatically)
PORT=5000


# -------------- CLIENT ENV (place in client/.env) --------------
# Only these go in client/.env with VITE_ prefix
# DO NOT put SERVICE_ROLE_KEY in client/.env
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
